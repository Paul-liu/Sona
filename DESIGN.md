# Sona · 设计调性规范（Design Language）

> Sona —— macOS 原生音乐播放器。本地与夸克云端双源聚合。
> 本规范基于 Apple 流体交互设计原则（WWDC）制定，是 Sona 全部界面决策的唯一依据。

---

## 一、设计哲学：声音的延伸

> 当界面对齐人的思考与运动方式，它就不再像一台计算机，而是无缝的延伸。

Sona 的调性一句话定义：**"深夜录音室里的一台精密仪器"** —— 黑亚克力的克制表面、氛围泛光的温度、波形流动的生命感。

三个关键词：

| 关键词 | 含义 | 界面落点 |
| --- | --- | --- |
| **克制**（Restrained） | 界面退居幕后，内容（封面、标题）是唯一主角 | 无纯色大色块、无装饰性分割线，全部用材质与留白分层 |
| **流动**（Fluid） | 界面行为模拟声音的物理属性：连续、携带动量、可随时打断 | 进度条拖拽跟手、均衡器跳动、切歌平滑过渡 |
| **温度**（Warm） | 云端来源的琥珀暖色与 Apple Red 的激情，中和石墨黑的冷 | 氛围泛光取自当前封面主色，随音乐呼吸 |

**内容优先（Content-First）** 原则：任何时候，视觉焦点排序永远是 `专辑封面 > 歌曲标题 > 播放控制 > 界面自身`。

---

## 二、色彩系统

### 2.1 表面层级（Surface Hierarchy）

| 层级 | 浅色模式 | 深色模式 | 材质 |
| --- | --- | --- | --- |
| L0 窗口基底 | `SystemWindowBackground` | `#161618` 石墨黑 | — |
| L1 侧边栏 | — | 基底 + 4% 白 | `.ultraThinMaterial` |
| L2 内容区 | `#FFFFFF` | `#1E1E20` | `.regularMaterial` |
| L3 播放条 | — | 基底 + 6% 白 | `.ultraThinMaterial` + 0.5pt 顶部微光分割 |
| L4 弹层（Sheet） | — | 基底 + 8% 白 | `.thickMaterial` |

规则：**层级越高（越接近用户），表面越亮、越不透明**。禁止用纯黑 `#000000`，最低只到 `#161618` —— 石墨黑保留了"材质厚度"的心理暗示。

### 2.2 强调色（Accent）

- **主强调**：`#FC3C44`（Apple Red）—— 进度条、播放主键、激活态、均衡器柱。全 App 唯一动作色，通过 `Color.accentColor` 全局注入。
- **云端辅色**：`#FF9F0A`（琥珀橙）—— 仅用于夸克来源标识与云端相关动效，暗示"来自云端的暖光"。

### 2.3 来源标识色（Source Badges）

| 来源 | 色值 | 底色 | 心理暗示 |
| --- | --- | --- | --- |
| 本地 | `#0A84FF` 沉静天蓝 | 12% 不透明度胶囊底 | 稳定、持久化、离线可用 |
| 夸克云端 | `#FF9F0A` 温暖琥珀 | 12% 不透明度胶囊底 | 实时拉取、云端传输 |

### 2.4 动态氛围泛光（Ambient Glow）

当前播放封面的主色调，经 80–120px 超大高斯模糊后作为播放条底层环境光，透明度 ≤ 18%。这是 Sona "温度" 的核心来源：**界面颜色随音乐变化，如同房间里被唱片封面的光染色**。

---

## 三、排版体系（Optical Typography）

全面采用系统字体：西文/数字 SF Pro，中文 PingFang SC。

| 层级 | 字号/字重 | 用途 | 光学规则 |
| --- | --- | --- | --- |
| 大标题 | 26pt Bold | 视图主标题（"全部歌曲"） | -0.5pt 字距（大字号收紧） |
| 歌曲主标题 | 13pt Regular~Semibold | 列表歌名、播放条标题 | 0 字距，默认行高 |
| 次要元数据 | 12pt Regular + `.secondary` | 艺人、专辑、大小 | 前景次级色，不降低字号 |
| 时长/时间戳 | 11pt Monospaced（SF Mono） | 进度时间、表格时长 | **等宽防跳动**——数字变化时宽度恒定 |
| 角标 | 9pt Bold Rounded | "本地"/"夸克"胶囊 | 全大写风格，加 0.3pt 字距提升可读性 |

铁律：**元数据靠颜色（secondary）区分层级，不靠缩小字号** —— 低于 11pt 的正文文字一律禁止。

---

## 四、动效规范（Motion · Apple 弹簧物理）

Sona 的动效哲学：**所有运动都是物理的，不是脚本演出的**。使用弹簧（spring）而非固定时长 transition，动画可随时打断、从实时位置续播、手势释放移交速度。

### 4.1 弹簧参数总表（对齐 WWDC 官方参数）

| 交互 | 阻尼比 damping | 响应 response | SwiftUI 落地 |
| --- | --- | --- | --- |
| 进度条拖拽跟手 | 1.0 | 0.25 | `.spring(response: 0.25, dampingFraction: 1.0)` |
| 播放/暂停按钮态变 | 1.0 | 0.3 | `.animation(.spring(0.3, 1.0), value: isPlaying)` |
| 切歌封面过渡 | 0.85 | 0.4 | 少量弹跳，模拟唱片落针 |
| 列表 Hover 高亮 | 1.0 | 0.15 | 临界阻尼，瞬时优雅 |
| Sheet 弹层（登录/导入） | 0.8 | 0.3 | 官方 sheet 参数，带轻微回弹 |
| 音量滑杆 | 1.0 | 0.2 | 跟手优先 |

### 4.2 三条运动铁律

1. **按下即反馈**（instant feedback）：任何可点区域，按下 0ms 内给出视觉响应（缩放 0.97 或高亮），不等抬起。
2. **可中断性**（interruptibility）：进度拖拽中途松手，动画从松手瞬间的实时位置与速度出发，绝不停帧重置。
3. **速度移交**（velocity handoff）：手势释放时把原始速度交给弹簧。拖拽进度条快甩 → 进度条带动量滑向目标；速度公式：`相对速度 = 释放速度 / (目标值 − 当前值)`。

### 4.3 生命感微动效

- **均衡器指示器**：当前播放曲目的序号位切换为 3 柱跳动均衡器（Timer 0.32s 驱动随机高度，easeInOut）—— 它是整个列表里唯一"活着"的元素。
- **封面落针**：切歌时新封面以 0.4s 弹簧从 0.96 缩放+淡入，模拟唱片机落针。
- **播放键反色**：主播放键纯黑/纯白实心圆底，按下瞬间 scale 0.92 → 弹回。

### 4.4 边界行为

进度条/音量条拖到端点采用**橡胶边界**（rubber banding）：越界位移按 `1 - 1/(x·c+1)` 衰减，松手弹回。禁止硬截断。

---

## 五、材质与深度（Materials & Depth）

- 侧边栏与播放条：`.ultraThinMaterial` —— 桌面壁纸微光渗透，暗示"悬浮"。
- 弹层：`.thickMaterial` —— 越上层越实，符合物理直觉。
- 阴影只给一个元素：播放条上的专辑封面（`radius: 4, y: 2`，柔和下坠感）。**禁止大面积投影** —— 深色 UI 中的阴影应像深夜中的微光，而非白昼中的实体。
- 分割线：0.5pt，`Color.primary.opacity(0.15)` —— 光的缝隙，而非实心线条。

---

## 六、图标（App Icon）

**概念**："从本地波形流向云朵" —— 一条极简平滑的音频波形线，向右平滑转折为云朵轮廓，隐喻本地与夸克云端的双源连通。

| 要素 | 规格 |
| --- | --- |
| 底板 | macOS Big Sur+ 圆角矩形（连续曲率，约 22.37% 圆角比），深灰亚克力渐变 `#26262A → #151517`，顶部内高光 8% |
| 波形线 | 3.5pt 圆头笔触，左侧居中振幅，4 个周期正弦衰减 |
| 云朵 | 波形末端自然放大为三瓣云轮廓（半径 90→130px 递增） |
| 渐变 | 线性 `#FC3C44 → #FF9F0A`（红→琥珀，从本地流向云端） |
| 发光 | 沿线 24px 内发光，透明度 35%，营造"通电"的生命感 |
| 尺寸 | 16~1024px 完整 iconset，最大 1024×1024 |

深浅色模式：Dock 深色环境为主场景，底板恒定为深亚克力（不做双版本，保持"录音室仪器"的恒定调性）。

---

## 七、无障碍（Accessibility）

| 场景 | 适配 |
| --- | --- |
| 减弱动效（Reduce Motion） | 均衡器改为静态双柱；封面过渡改为纯淡入；禁用橡胶边界弹回 |
| 降低透明度（Reduce Transparency） | `.ultraThinMaterial` 自动降级为实色 `#1E1E20`（系统行为，需保证实色下对比度仍达标） |
| VoiceOver | 进度条 `accessibilityLabel("播放进度")` + `accessibilityValue("3分20秒，共4分05秒")`；来源角标读出"来源：本地/夸克网盘" |
| 键盘 | 完整键盘可达：空格播放/暂停、⌘O 导入、方向键切歌 |
| 对比度 | 次要文本 `.secondary` 在深色基底上 ≥ 4.5:1（`#98989F` on `#161618` 实测 7.2:1） |

---

## 八、八大设计原则校验

| 原则 | Sona 兑现方式 | 状态 |
| --- | --- | --- |
| 目的性 | 每个动效都有功能含义（均衡器=正在播放，泛光=当前封面） | ✅ |
| 用户控制权 | 进度任意拖拽、动画可中断、速度移交 | ✅ |
| 责任 | 无打断式弹窗，播放状态全局可预测 | ✅ |
| 熟悉感 | NavigationSplitView + 底部播放条 = macOS 音乐类 App 心智模型 | ✅ |
| 灵活性 | 键盘/鼠标/媒体键/控制中心四通道操控 | ✅ |
| 简洁 | 唯一动作色 #FC3C44，其余全部材质与留白 | ✅ |
| 工艺 | 等宽时间戳防跳动、0.5pt 光缝分割、落针动效 | ✅ |
| 愉悦感 | 封面氛围泛光、跳动的均衡器、云朵图标 | ✅ |

---

*Sona Design Language v1.0 · 2026-09-06 · 基于 Apple 流体交互设计原则*
